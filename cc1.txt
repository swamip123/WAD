sudo apt update   #update ubuntu
sudo apt upgrade  #update ubuntu

sudo apt install apt-transport-https ca-certificates gnupg curl   #Install Required Packages

curl https://packages.cloud.google.com/apt/doc/apt-key.gpg | sudo apt-key add -
echo "deb https://packages.cloud.google.com/apt cloud-sdk main" | sudo tee -a /etc/apt/sources.list.d/google-cloud-sdk.list  #Add Google Cloud SDK Repository

sudo apt update
sudo apt install google-cloud-sdk   #Install Google Cloud SDK

gcloud version  #Check Installation

gcloud init  #Initialize SDK


mkdir hello-python
cd hello-python    #Create Folder

nano app.py

from flask import Flask

app = Flask(__name__)

@app.route('/')
def home():
    return "Hello World from Google App Engine"

if __name__ == '__main__':
    app.run()



nano requirements.txt   #Create requirements.txt
Flask==2.3.2
gunicorn==21.2.0

pip install flask gunicorn

nano app.yaml   #create app.yaml
runtime: python39

python3 app.py  #Run Application
